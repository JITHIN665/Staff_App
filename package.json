{
  "name": "staff-app-server",
  "version": "1.0.0",
  "description": "Mock JSON server for Staff App",
  "main": "db.json",
  "scripts": {
    "start": "json-server --watch db.json --port 3000",
    "dev": "json-server --watch db.json --port 3000 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "devDependencies": {},
  "keywords": ["json-server", "mock-api", "staff-app"],
  "author": "Staff App Team",
  "license": "MIT"
}
